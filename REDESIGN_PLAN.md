# 편제 변경 이력 화면 개편 계획

## 1. 목표

현재 `index.html`의 연도별 조직 배치와 연결선 계산은 유지하면서 화면 디자인과 정보 밀도를 개선한다.

이번 작업은 조회 화면 개편까지만 포함한다. 편제개편 입력 폼, SQLite, 저장·수정 API는 후속 단계로 미룬다.

## 1-1. RALPLAN-DR 요약

### 원칙

- 기존 스냅숏 생성, 정렬, 학부·학과 묶음, carry edge, 변경 관계 edge, 검색, 계보 강조 동작을 보존한다.
- 이번 단계는 조회 전용 화면 개편으로 한정하고 입력 폼, SQLite, 저장·수정 API는 포함하지 않는다.
- 레이아웃 숫자는 먼저 상수화한 뒤 카드 밀도와 시각 표현을 바꾼다.
- 새 의존성을 추가하지 않고 첫 구현은 `index.html` 안에서 작게 진행한다.
- 툴팁과 상세 패널 상태는 좌표 계산과 분리한다.

### 결정 기준

- 그래프 배치와 연결선 회귀 위험을 가장 낮게 유지한다.
- 긴 연도 범위에서도 정보 밀도와 가독성을 개선한다.
- 단일 파일 기반의 현재 구조에서 변경을 작고 되돌리기 쉽게 유지한다.

### 검토한 선택지

| 선택지 | 장점 | 단점 | 판단 |
| --- | --- | --- | --- |
| 레이아웃 상수화 후 단일 파일에서 점진 개편 | 기존 계산 보존이 쉽고 diff가 작다 | `index.html`이 계속 커진다 | 이번 단계 채택 |
| DOM/CSS를 먼저 바꾸고 상수화는 나중에 진행 | 화면 변화가 빠르게 보인다 | 카드, 연결선, 학부 박스 좌표가 어긋날 위험이 크다 | 배제 |
| CSS/JS 파일 분리까지 함께 진행 | 장기 유지보수성이 좋아진다 | 조회 화면 개편 범위를 넘어가고 회귀 원인 분리가 어렵다 | 후속 후보 |

### 세부 대안 결정

| 쟁점 | 선택 | 대안 | 이유 |
| --- | --- | --- | --- |
| CSV 자동 로딩 실패 대응 | HTTP 서버 안내만 표시 | 파일 선택 fallback 유지 | 이번 단계에서 CSV 업로드 UI를 제거하기로 했으므로 수동 로딩 경로도 함께 제거한다 |
| 연도 sticky 헤더 | 같은 `.canvas-wrap` 스크롤 문맥 안에 배치 | 별도 헤더를 만들고 scrollLeft 동기화 | 별도 동기화는 어긋남과 이벤트 관리 부담이 커진다 |
| 상세 패널 데이터 | 기존 `state.nodes`, `state.edges`, `state.units` 재사용 | 패널 전용 노드/관계 복제 저장 | 중복 상태는 stale 데이터 위험을 만든다 |
| 변경 유형 필터 | 제거하고 전체 보기 기준 유지 | 필터 UI를 축소해서 유지 | 현재 필터는 그래프 구성 자체를 바꾸므로 이번 조회 개편 범위를 흐린다 |

## 2. 이번 작업 범위

### 포함

- 같은 폴더의 CSV 자동 로딩
- 시작·종료 학년도 필터
- 조직명·조직 코드 검색
- 연도별 세로 열 형태의 흐름도
- 세로 스크롤 중 항상 보이는 연도 헤더
- 간결한 직사각형 조직 카드
- 같은 학부·학과 소속 조직 묶음
- 조직 툴팁
- 노드 선택 시 우측 상세 패널
- 기존 연결 계보 강조 기능
- 기존 배치 순서와 연결선 경로 보존

### 제외

- 요약 지표 영역
- 변경 유형 필터
- 대학·학부 필터
- CSV 업로드 UI
- 편제개편 입력 폼
- SQLite 도입
- 저장·수정·삭제 API

변경 유형 필터는 현재 그래프에 포함되는 관계 자체를 줄이는 기능이다. 이번 개편에서는 이 기능을 의도적으로 제거하며, 배치·연결선 회귀 보존 기준은 변경 유형을 필터링하지 않은 전체 보기 상태에 적용한다.

## 3. 데이터 운영 방식

이번 단계는 조회 전용이므로 다음 파일을 같은 폴더에서 자동으로 읽는다.

- `dim_org_unit.csv`
- `org_unit_relation.csv`

페이지가 열리면 두 CSV를 자동으로 불러오고 바로 렌더링한다. 파일 선택 입력과 `그리기` 버튼은 제거한다.

CSV 자동 로딩에는 HTTP 서버가 필요하다. `file://`로 직접 여는 방식은 지원 대상으로 삼지 않는다.

자동 로딩 실패 시에는 HTTP 서버로 열어야 한다는 안내를 보여준다. CSV 업로드 UI, `FileReader` fallback, 파일 선택 기반 수동 로딩은 이번 단계에서 함께 제거한다.

입력 폼을 개발하는 후속 단계에서는 SQLite를 원본 저장소로 도입하고 CSV는 가져오기·내보내기 형식으로 전환한다.

## 4. 반드시 유지할 기존 로직

디자인을 변경하더라도 다음 동작과 계산 결과는 유지한다.

- 변경 이력을 기반으로 연도별 조직 스냅숏을 생성하는 방식
- 선택한 시작 학년도보다 한 해 앞선 상태부터 표시하는 방식
- 단과대학 → 학부·학과 → 전공 순의 정렬
- 같은 단과대학과 학부·학과에 속한 전공을 묶는 방식
- 연도별 그룹 높이를 맞추어 열 사이의 세로 대응을 유지하는 방식
- 연결된 조직이 연도 사이에서 가능한 한 유사한 세로 위치를 갖는 배치
- 변경되지 않은 조직을 다음 연도로 이어주는 carry edge
- 분리·통합·개편 등 변경 관계의 곡선 연결선
- 노드 선택 시 연결된 전체 계보를 강조하는 기능
- 조직명·코드 검색 시 관련 노드와 연결선을 강조하는 기능

특히 다음 함수는 원칙적으로 알고리즘을 변경하지 않는다.

- `buildSnapshots()`
- `buildLayoutContext()`
- `buildYPositions()`
- `groupLaneItems()`
- `sortKeyForNode()`
- `buildCarryEdges()`
- `drawEdges()`

표현 변경은 주로 CSS와 `render()`, `drawNode()`, `drawDeptBoxes()`의 DOM 생성 부분에 한정한다.

## 5. 화면 구조

```text
┌─────────────────────────────────────────────────────────┐
│ 편제 변경 이력                          데이터 기준일     │
├─────────────────────────────────────────────────────────┤
│ 시작 학년도  종료 학년도   조직명·코드 검색    초기화    │
├─────────────────────────────────────────────────────────┤
│       2021학년도      2022학년도      2023학년도 ...     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                 편제 변경 흐름도                         │
│                                             ┌─────────┐ │
│                                             │ 상세 패널│ │
│                                             └─────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 6. 상단 헤더와 필터 바

### 상단 헤더

- 서비스명: `편제 변경 이력`
- 데이터 기준일 또는 CSV 최종 갱신일
- 흰색 배경과 얇은 하단 경계선
- 작은 글자와 낮은 채도의 보조 정보

### 필터 바

- 시작 학년도
- 종료 학년도
- 조직명·조직 코드 검색
- 필터 초기화

학년도 변경 시 즉시 다시 렌더링한다. 검색은 현재처럼 입력 즉시 반영한다.

## 7. 고정 연도 헤더

현재 캔버스 내부의 절대 위치 연도 라벨을 흐름도 본문 상단의 sticky 헤더 행으로 분리한다.

구조는 다음과 같이 구성한다.

```text
가로·세로 스크롤 컨테이너
  ├─ sticky 연도 헤더
  └─ 흐름도 캔버스
```

요구사항:

- 세로 스크롤 중 연도가 항상 상단에 보여야 한다.
- 가로 스크롤 시 연도 헤더와 노드 열이 함께 움직여야 한다.
- 연도 헤더는 기존 `xByYear` 좌표를 그대로 사용한다.
- 기존 열 너비와 연도 간격 계산은 유지한다.
- 필터 바도 고정할 경우 연도 헤더는 필터 바 바로 아래에 위치한다.
- 별도 스크롤 동기화보다 동일 스크롤 컨테이너를 우선 사용한다.

구현 경계:

- 연도 헤더는 `.canvas-wrap`과 같은 가로 스크롤 문맥 안에 둔다.
- 연도 헤더의 내부 폭은 캔버스 폭과 동일하게 맞춘다.
- 연도 라벨 위치는 `render()`에서 만든 `xByYear` 값을 그대로 사용한다.
- 연도 라벨은 캔버스 내부가 아니라 별도 헤더 컨테이너에서 생성·정리한다.
- `canvas.querySelectorAll()` 기반 정리 대상에서 `.year-label`을 분리해 헤더와 본문 정리 책임을 나눈다.

## 8. 조직 카드

카드에는 조직명만 기본 표시한다.

```text
┌──────────────────┐
│ 데이터사이언스전공 │
└──────────────────┘
```

다음 정보는 카드에서 제거하고 툴팁 또는 상세 패널로 옮긴다.

- 조직 코드
- 단과대학명
- 전체 상위 조직 경로
- 설명과 비고

권장 크기:

- 너비: 기존 178px을 우선 유지
- 높이: 44~50px
- 노드 세로 간격: 58~64px
- 학부 박스 상하 여백: 28~32px

너비를 먼저 변경하면 연결선 시작점과 연도 간격에 영향이 크므로, 첫 작업에서는 높이와 내부 정보량만 줄인다.

긴 조직명은 한 줄 말줄임표를 우선 사용하며 전체 이름은 툴팁과 상세 패널에 표시한다.

카드 크기와 연결선 기준점은 DOM 생성 코드 안의 숫자로 직접 흩어두지 않고 상수로 분리한다.

- 노드 너비
- 노드 높이
- 노드 가로 중심 보정값
- 노드 세로 중심 보정값
- 노드 세로 간격
- 학부 박스 상하 여백
- 연결선 시작·종료 기준점

카드 높이를 줄이는 작업은 이 상수들을 먼저 분리한 뒤 진행한다. CSS 카드 높이만 변경해 연결선과 학부 박스 위치가 어긋나는 상황을 피한다.

## 9. 학부·학과 묶음

기존 `drawDeptBoxes()`의 그룹 조건을 유지한다.

- 같은 연도
- 같은 단과대학
- 같은 학부·학과

표현만 다음처럼 정리한다.

- 얇은 실선 테두리
- 흰색에 가까운 중립 배경
- 박스 상단의 작은 학부·학과명 라벨
- 전공 카드보다 약한 시각적 강조
- 전공이 없고 조직이 하나뿐인 경우 별도 박스를 만들지 않는 현재 조건 유지

단과대학별 큰 화면 구획이나 고정 라벨은 추가하지 않는다.

## 10. 툴팁

모든 조직 노드에서 툴팁을 제공한다.

- 조직명
- 조직 코드
- 단과대학
- 학부·학과
- 조직 유형
- 존속기한이 있을 경우 존속기한

현재 일부 변경 관계에만 나타나는 존속기한 툴팁을 일반 조직 정보 툴팁으로 확장한다.

## 11. 우측 상세 패널

노드를 클릭하면 기존 계보 강조와 동시에 우측 상세 패널을 연다.

표시 정보:

- 선택 학년도
- 조직명과 코드
- 조직 유형
- 단과대학 → 학부·학과 → 전공 경로
- 직전 조직
- 이후 조직
- 변경 유형
- 존속기한
- 비고

동작:

- 선택한 노드와 연결된 전체 계보 강조
- 관련 없는 노드와 연결선 흐리게 표시
- 같은 노드를 다시 클릭하거나 닫기 버튼을 누르면 패널 닫기
- 데스크톱에서는 우측 고정 오버레이
- 좁은 화면에서는 하단 드로어
- 패널을 열고 닫을 때 캔버스 좌표와 정렬을 다시 계산하지 않음

상세 패널에 필요한 정보는 DOM 이벤트 안에서 직접 조합하지 않고 선택 노드 기준 파생 데이터 함수에서 만든다.

예상 함수:

- `getNodeDetails(nodeKey)`

반환 정보:

- 선택 노드
- 선택 학년도
- 조직 기본 정보
- 전체 상위 경로
- 선택 노드로 들어오는 변경 관계
- 선택 노드에서 나가는 변경 관계
- 직전 조직 목록
- 이후 조직 목록
- 관련 변경 유형, 존속기한, 비고

이 함수는 이미 렌더링된 `state.nodes`, `state.edges`, `state.units`를 조회만 하며, 좌표 계산이나 DOM 갱신을 수행하지 않는다.

현재 `index.html`은 렌더링 후 `state.nodes`에 노드 배열을 저장한다. 상세 패널은 이 기존 상태를 우선 재사용하고, keyed lookup이 필요한 경우에만 `state.nodeMap`을 추가한다. 동일한 노드 정보를 별도 구조에 중복 저장하지 않는다.

## 12. 연결선과 범례

연결선 경로 계산은 유지하고 스타일만 정리한다.

- 기본 연결선은 얇고 낮은 채도 사용
- 선택되거나 검색된 계보만 선명하게 강조
- carry edge는 가장 연한 중립색 사용
- 화살표 크기를 줄여 카드보다 덜 두드러지게 처리
- 연결선은 학부 박스보다 위, 조직 카드보다 아래에 배치
- 변경 유형 라벨은 밀도를 검토해 항상 표시하거나 선택·호버 시 강조

변경 유형 필터는 제거하지만 선 색상의 의미는 유지한다. 범례는 흐름도 우측 상단의 작은 범례 또는 도움말 팝오버로 제공한다.

## 13. 구현 순서

### 1단계: 레이아웃 상수 분리

- 변경 전 기본 전체 보기의 연도별 노드 코드 순서, edge 수, carry edge 수, 대표 SVG path 문자열을 baseline으로 기록
- 노드 너비·높이·중심 보정값을 상수로 분리
- 노드 세로 간격과 학부 박스 여백을 상수로 분리
- 연결선 시작·종료 기준점을 노드 상수에서 계산
- 기존 렌더링 결과가 변하지 않는지 먼저 확인

### 2단계: UI 구조 분리

- CSV 업로드 UI 제거
- 변경 유형 필터 제거
- 변경 유형 필터 제거는 전체 보기 기준으로 그래프를 고정하는 의도적 기능 축소임을 주석 또는 계획 근거로 남김
- `typeFilter` 관련 DOM 참조, 이벤트 리스너, `fillSelect(typeFilter, ...)`, `selectedType`, 변경 유형별 relation filter 분기를 함께 제거
- 필터 바 재구성
- 연도 헤더 영역과 캔버스 본문 분리
- 동일 폴더 CSV 자동 로딩만 유지
- CSV 업로드 관련 markup, DOM 참조, 이벤트 리스너, `updateLoad()` 의존성, `FileReader` fallback, fallback 안내 문구를 한 번에 정리

### 3단계: 디자인 토큰 정리

- 색상, 경계선, 간격, 글자 크기를 CSS 변수로 정리
- 저채도 색상과 얇은 구분선 적용
- 헤더, 필터, 카드, 학부 박스 스타일 통일

### 4단계: 카드 밀도 개선

- 카드에서 상위 경로 제거
- 카드 높이와 노드 세로 간격 축소
- 학부 박스 여백 축소
- 긴 이름 말줄임 처리
- 기존 배치 순서와 연결선 상태 비교

### 5단계: 고정 연도 헤더

- 기존 `xByYear` 좌표 재사용
- 세로 스크롤 중 sticky 동작 구현
- 가로 스크롤 시 열 중심 일치 확인
- 필터 바와 연도 헤더의 고정 위치 조정

### 6단계: 툴팁과 상세 패널

- 모든 노드에 공통 툴팁 적용
- 단과대학과 전체 경로 표시
- 선택 노드 기준 상세 정보를 만드는 `getNodeDetails()` 성격의 파생 함수 추가
- 우측 상세 패널 추가
- 기존 계보 강조 기능과 패널 상태 결합

### 7단계: 회귀 검증

- 기본 전체 보기에서 연도별 노드 코드 순서가 변경 전과 같은지 비교
- 기본 전체 보기에서 변경 관계 edge 수와 carry edge 수가 의도치 않게 변하지 않았는지 비교
- 대표 edge의 source/target node key와 path 좌표 문자열이 상수화 전후 동일한지 비교
- 연도 헤더 라벨의 중심 x 좌표가 같은 연도의 노드 중심 x 좌표와 일치하는지 확인
- 조직명 검색과 조직 코드 검색에서 focus/dimmed 클래스 적용이 정상인지 확인
- 노드 선택 시 연결 계보 전체가 강조되고 같은 노드 재클릭 또는 배경 클릭 시 해제되는지 확인
- 시작·종료 학년도 변경 시 `minYear - 1`부터 `maxYear`까지 표시되는지 확인
- 분리·통합·개편 관계와 carry edge가 모두 표시되는지 대표 사례로 확인
- HTTP 서버에서 자동 CSV 로딩이 성공하는지 확인
- `file://` 또는 CSV 로딩 실패 상황에서 HTTP 서버 안내 메시지가 표시되는지 확인
- 세로 스크롤 중 연도 헤더가 보이고, 가로 스크롤 중 헤더와 노드 열이 함께 움직이는지 확인
- 데스크톱 폭에서는 우측 상세 패널, 좁은 화면에서는 하단 드로어가 열리고 그래프 좌표가 움직이지 않는지 확인
- 브라우저 콘솔에 JavaScript 오류가 없는지 확인

## 13-1. 리스크와 완화

| 리스크 | 영향 | 완화 |
| --- | --- | --- |
| 카드 높이만 줄여 연결선과 학부 박스가 어긋남 | edge endpoint와 박스 경계가 시각적으로 불일치 | `LAYOUT` 상수 추출 후 기존값으로 1차 검증하고, 그 다음 밀도 값을 조정 |
| 변경 유형 필터 제거를 회귀로 오해 | 기존 필터별 그래프와 결과가 달라 보임 | 회귀 기준을 전체 보기로 명시하고 `typeFilter` 제거 타깃을 한 번에 정리 |
| sticky 연도 헤더와 캔버스 열 중심 불일치 | 긴 가로 스크롤에서 연도와 노드 열이 어긋남 | 같은 `.canvas-wrap` 스크롤 문맥, 동일 폭, 동일 `xByYear` 값 사용 |
| 상세 패널 상태가 오래된 노드 정보를 표시 | 학년도 범위 변경 후 잘못된 상세 정보 노출 | `render()` 후 갱신되는 기존 `state.nodes`를 기준으로 `getNodeDetails()` 계산 |
| 패널 열림/닫힘이 그래프 레이아웃을 흔듦 | 선택 시 좌표와 연결선이 움직여 사용성이 나빠짐 | 패널은 fixed overlay 또는 drawer로 구현하고 open/close에서 `render()`를 호출하지 않음 |
| CSV 자동 로딩 실패 시 사용자가 멈춤 | 파일 선택 fallback 제거 후 빈 화면처럼 보일 수 있음 | HTTP 서버 필요 안내와 로딩 실패 상태 문구를 명확히 표시 |
| 검색과 선택 강조가 서로 덮어씀 | focus/dimmed 상태가 예측 불가능 | `applySearch()`를 단일 클래스 토글 경로로 유지하고 선택/검색 조합을 대표 케이스로 검증 |

## 14. 완료 조건

- 페이지 진입 시 폴더의 CSV를 자동으로 읽는다.
- 파일 업로드 없이 흐름도가 표시된다.
- 화면에는 학년도 필터와 조직명·코드 검색만 제공된다.
- 세로 스크롤 위치와 관계없이 연도 헤더가 보인다.
- 가로 스크롤 중 연도 헤더와 조직 열의 중심이 일치한다.
- 기존과 동일한 조직 순서와 학부 묶음이 유지된다.
- 카드 높이가 줄어 전체 흐름도의 세로 길이가 감소한다.
- 카드에는 조직명만 표시되고 나머지 정보는 툴팁과 상세 패널에서 확인할 수 있다.
- 노드 선택 시 상세 패널과 연결 계보 강조가 함께 동작한다.
- 패널을 열어도 기존 노드 좌표와 연결선 경로가 움직이지 않는다.
- 검색, 학년도 범위 변경, 노드 선택·해제가 정상 동작한다.

## 15. 후속 작업

조회 화면 개편이 안정화된 뒤 별도 계획으로 진행한다.

- 편제개편 입력 폼
- 개편 이벤트와 이전·이후 조직을 분리한 데이터 모델
- SQLite 도입
- 조회·등록·수정·삭제 API
- 기존 CSV의 초기 마이그레이션
- CSV 가져오기·내보내기
- 변경 이력과 데이터 백업
